
public class C008MetodosString2 {
    public static void main(String[] args) {
/*
        //métodos de Strings en Java:

        //length(): Devuelve la longitud de la cadena.

        String str = "Hola Mundo";
        int len = str.length(); // len = 10

        //charAt(): Devuelve el carácter en el índice especificado.

        String str = "Hola Mundo";
        char ch = str.charAt(1); // ch = 'o'

        //concat(): concatena una cadena al final de otra cadena.

        String str1 = "Hola";
        String str2 = "mundo";
        String str3 = str1.concat(str2); // str3 = "Holamundo"

        //substring(): Devuelve una subcadena de la cadena.

        String str = "Hola Mundo";
        String sub = str.substring(4); // sub = " Mundo"

        //equals(): Compara dos cadenas y devuelve true si son iguales.

        String str1 = "Hola";
        String str2 = "Hola";
        boolean equal = str1.equals(str2); // equal = true

        //equalsIgnoreCase(): Compara dos cadenas sin importar mayúsculas o minúsculas.

        String str1 = "Hola";
        String str2 = "hola";
        boolean equal = str1.equalsIgnoreCase(str2); // equal = true

        //compareTo(): compara dos cadenas y devuelve un número negativo, cero o positivo según si la primera cadena es menor, igual o mayor que la segunda cadena.

        String str1 = "Hola";
        String str2 = "Hola";
        int result = str1.compareTo(str2); // result = 0

        //contains(): Devuelve true si la cadena contiene la subcadena especificada.

        String str = "Hola Mundo";
        boolean contain = str.contains("Mundo"); // contain = true

        //indexOf(): Devuelve el índice de la primera ocurrencia de la subcadena especificada.

        String str = "Hola Mundo";
        int index = str.indexOf("Mundo"); // index = 5

        //lastIndexOf(): Devuelve el índice de la última ocurrencia de la subcadena especificada.

        String str = "Hola Mundo";
        int index = str.lastIndexOf("o"); // index = 7

        //trim(): elimina los espacios en blanco al inicio y al final de la cadena.

        String str = "  Hola mundo   ";
        String trimStr = str.trim(); // trimStr = "Hola mundo"

        //replace(): Reemplaza todas las ocurrencias de una subcadena por otra.

        String str = "Hola Mundo";
        String newStr = str.replace("o", "a"); // newStr = "Hala Munda"

        //toUpperCase(): Convierte la cadena en mayúsculas.

        String str = "Hola Mundo";
        String upper = str.toUpperCase(); // upper = "HOLA MUNDO"

        //toLowerCase(): convierte la cadena en minúsculas.

        String str = "Hola mundo";
        String lowerStr = str.toLowerCase(); // lowerStr = "hola mundo"

        //startsWith(): devuelve verdadero si la cadena comienza con la subcadena especificada.

        String str = "Hola mundo";
        boolean startsWith = str.startsWith("Hola"); // startsWith = true

        //endsWith(): devuelve verdadero si la cadena termina con la subcadena especificada.

        String str = "Hola mundo";
        boolean endsWith = str.endsWith("mundo"); // endsWith = true

        //isEmpty(): devuelve verdadero si la cadena está vacía.

        String str = "";
        boolean isEmpty = str.isEmpty(); // isEmpty = true

        //contains(): devuelve verdadero si la cadena contiene la subcadena especificada.

        String str = "Hola mundo";
        boolean contains = str.contains("mundo"); // contains = true

        //split(): divide la cadena en una matriz de subcadenas utilizando un delimitador especificado.

        String str = "Hola,mundo,cruel";
        String[] subStrs = str.split(","); // subStrs = {"Hola", "mundo", "cruel"}

        //format(): devuelve una cadena formateada utilizando un formato especificado.

        String str = "Hola";
        int num = 5;
        String formattedStr = String.format("%s, el número es %d", str, num); // formattedStr = "Hola, el número es 5"

        //join(): une una matriz de cadenas en una sola cadena utilizando un delimitador especificado.

        String[] arr = {"Hola", "mundo", "cruel"};
        String str = String.join(" ", arr); // str = "Hola mundo cruel"

        //strip(): elimina los espacios en blanco al inicio y al final de la cadena, incluidos los espacios en blanco Unicode.

        String str = "   Hola mundo   ";
        String stripStr = str.strip(); // stripStr = "Hola mundo"

        //stripLeading(): elimina los espacios en blanco al inicio de la cadena, incluidos los espacios en blanco Unicode.

        String str = "   Hola mundo   ";
        String stripLeadingStr = str.stripLeading(); // stripLeadingStr = "Hola mundo   "

        //stripTrailing(): elimina los espacios en blanco al final de la cadena, incluidos los espacios en blanco Unicode.

        String str = "   Hola mundo   ";
        String stripTrailingStr = str.stripTrailing(); // stripTrailingStr = "   Hola mundo"

        //repeat(): devuelve una cadena que se repite un número especificado de veces.

        String str = "Hola";
        String repeatedStr = str.repeat(3); // repeatedStr = "HolaHolaHola"

        //matches(): devuelve verdadero si la cadena coincide con una expresión regular especificada.

        String str = "123";
        boolean matches = str.matches("\\d+"); // matches = true

        //replaceAll(): reemplaza todas las ocurrencias de una expresión regular en la cadena original.

        String str = "Hola mundo";
        String replacedStr = str.replaceAll("\\s", ""); // replacedStr = "Holamundo"

        //replaceFirst(): reemplaza la primera ocurrencia de una expresión regular en la cadena original.

        String str = "Hola mundo";
        String replacedStr = str.replaceFirst("\\s", ""); // replacedStr = "Holamundo"

        //subSequence(): devuelve una subsecuencia de la cadena original como una instancia de CharSequence.

        String str = "Hola mundo";
        CharSequence subSeq = str.subSequence(0, 4); // subSeq = "Hola"

        //codePointAt(): devuelve el valor Unicode del carácter en el índice especificado.

        String str = "Hola mundo";
        int codePoint = str.codePointAt(1); // codePoint = 111

        //codePointCount(): devuelve el número de valores Unicode en una subcadena de la cadena original.

        String str = "Hola mundo";
        int codePointCount = str.codePointCount(0, 4); // codePointCount = 4

        //offsetByCodePoints(): devuelve el índice del carácter que está offset puntos de código más allá del índice especificado.

        String str = "Hola mundo";
        int index = str.offsetByCodePoints(0, 3); // index = 3

 */
    }
}
